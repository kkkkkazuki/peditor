# 開発計画（2026-08-20 時点）

`CLAUDE.md` = 設計思想と守るべき前提。
このファイル = **現状の実測結果**と**次に手を動かす順番**。作業したらここのチェックを更新する。

## 1. 現状サマリ

| 項目 | 値 |
|---|---|
| 構成 | `index.html` 単一ファイル・545行（ビルド無し） |
| 依存 | pdf.js 3.11.174 / pdf-lib 1.17.1 / @pdf-lib/fontkit 1.1.1（すべてCDN、バージョン固定済み） |
| 履歴 | `cd8a298 v0.1` → `18a0b8b 日本語フォント埋め込み` |
| 中核設計 | `S.order = [{s,p,r}]` の配列操作だけで編集し、PDF実体の組み立ては保存・印刷・抽出の瞬間だけ（`buildPdf()`） |

中核設計は健全で、このまま維持する。以下の計画はすべて「`S.order` を壊さない」ことを前提にしている。

## 2. 実装状況（コードを読んだ実測）

| 機能 | 状態 | 実体 |
|---|---|---|
| 表示（描画・フィット・DPR対応） | 済 | `showPage()` |
| サムネイル＋キャッシュ | 済 | `buildThumbs()` / `paintThumb()` / `thumbCache` |
| 複数選択＋ドラッグ並び替え | 済・検証済 | `moveSelectionTo()` |
| 削除 / 回転 / PDF追加 / 抽出 | 済 | `deletePages()` `rotate()` `buildPdf()` |
| Undo / Redo | **要修正** | 「PDFを追加」を挟むと壊れる（B-1） |
| 保存（FS Access API＋DLフォールバック） | 済 | `saveBytes()` |
| 印刷 | 済（粗い） | `btnPrint`（B-5） |
| 日本語フォント埋め込み | **基盤のみ** | `embedJpFont()` は定義済みだが**呼び出し箇所が無い**。使う側（書き込み）が未実装のため |
| 文字・図形の書き込み | 未着手 | — |
| PWA化 / Tauri化 | 未着手 | — |

並び替えロジックは代表ケースを机上実行して確認済み（複数選択を先頭/末尾/中間へ、飛び石選択、自分自身の前後へのドロップ＝変化なし）。すべて期待通りで、**連続D&Dで順序がズレる問題は無い**。

## 3. 見つかった不具合・リスク（優先度順）

### B-1【高】「PDFを追加」の後に元に戻すと例外で落ちる
`index.html:463-481`。`snapshot()` した直後に `loadFiles()`（`index.html:197-214`）が `S.undo = []` で履歴配列ごと捨てるため、後段の `S.undo.length = before` が**空配列を穴だらけに引き伸ばすだけ**になる。結果 `S.undo` は `[<empty>]` となり、Undoボタンは有効に見えるのに `undo()` で `S.order = undefined` → `TypeError`。

再現（ロジックのみ抜き出して実行した結果）:
```
undo len: 1  内容: [null]
undo()でpopされる値: undefined
→ 例外: TypeError Cannot read properties of undefined (reading 'length')
```

- 修正方針: `loadFiles(files, {mode:"replace"|"append"})` に分け、**履歴と `S.order` の書き換えは呼び出し側に集約**する。`loadFiles` は「ソースを読んでページ配列を返す」だけの純粋な役割にする。`S.undo.length = before` のような後始末ハックは消す。

### B-2【高】エラー処理がどこにも無い
壊れたPDF・暗号化PDFを掴むと `loadFiles` の `await` が reject し、ステータスが「読み込み中…」のまま固まる（`try/catch` 無し）。
- 修正方針: ファイル単位で `try/catch`。暗号化は前提上「非対応」でよいので、`PasswordException` を判別して**「パスワード付きPDFには対応していません」と明示**する。複数ファイル読み込み時は、失敗したファイルだけスキップして残りは開く。

### B-3【中】編集内容が予告なく消える
`btnOpen` とウィンドウへのファイルドロップ（`index.html:531-538`）は、確認なしに `S.sources` / `S.order` を破棄する。`beforeunload` も無いのでタブを閉じても警告が出ない。
- 修正方針: 「未保存の編集がある」フラグ（`S.dirty`）を持ち、破棄操作前に確認、`beforeunload` を登録。ドロップは**既存があれば「追加」を既定**にした方が事故が少ない（要判断・§6）。

### B-4【中】サムネイルDOMを毎回全再生成している
`refresh()` → `buildThumbs()`（`index.html:276-293`）が `box.textContent = ""` で全消し・全再構築する。`refresh()` は**クリック1回ごとに走る**ため、ページ数に比例したDOM生成が選択のたびに発生する。画像は `thumbCache` で守られているが、DOM生成と `paintThumb` の同時起動は守られていない。
- 修正方針: (a) 選択状態の変化はクラス付け替えだけで済ませ、`buildThumbs` は件数が変わった時だけ。(b) `IntersectionObserver` で見えているサムネだけ描画。数百ページでの体感はここで決まる。

### B-5【中】印刷まわりの後始末
`index.html:502-510`。blob URL を `revokeObjectURL` していない（印刷のたびにリーク）、`printFrame` は `hidden=false` のまま、`contentWindow.print()` は Firefox で失敗しうるのに握り潰している。
- 修正方針: `onload` 後に revoke、`catch` でウィンドウを開くフォールバックを明示。

### B-6【低】`showPage()` の競合
`index.html:311-335`。`getPage()` を await している間に別の `showPage()` が入ると、`renderTask.cancel()` をすり抜けて同じcanvasに2つ描かれうる。世代カウンタで「自分が最新か」を確認してから描く。

### B-7【低】ファイル差し替え時に旧 pdf.js ドキュメントを `destroy()` していない
大きいPDFを開き直すたびにワーカー側のメモリが残る。

### B-8【低】サムネ枠が A4 縦固定（`aspect-ratio:1/1.414`）
横向きページや非A4が枠内で小さく見える。実ページ比率を使うか、枠を可変にする。

## 4. ロードマップ

### Phase 0 — 足回りの修正（最優先・小さい）
- [ ] B-1 `loadFiles` の責務分割とUndo修復
- [ ] B-2 読み込みエラー処理＋暗号化PDFの明示メッセージ
- [ ] B-3 未保存ガード（`S.dirty` / `beforeunload`）
- [ ] B-5 印刷の後始末

書き込み機能を載せる前にここを片付ける。**書き込みはUndo履歴の上に乗るので、壊れた履歴の上に積むと原因切り分けが困難になる。**

### Phase 1 — 書き込みレイヤー（本命）

`CLAUDE.md` の案では `S.annotations = { [orderIndex]: [...] }` としているが、**orderIndex は削除・並び替えで全部ずれる**（1ページ消すと以降の注釈が1ページ前にワープする）。ここは変更する:

```js
// 注釈は order の要素そのものに持たせる（インデックスに依存させない）
S.order = [{ s, p, r, a: [ {type:'text', x, y, size, text, color}, ... ] }]
```
- こうすると並び替え・削除・複製で注釈が**自動的にページに追従**する。
- `snapshot()` は現在 `{...o}` の浅いコピーなので、**`a` を deep copy に変える**こと（でないとUndoで注釈が戻らない）。
- 座標は**回転前のPDF点座標（pt, 左下原点）で保存**する。画面座標→PDF座標の変換は `page.getViewport()` の `convertToPdfPoint()` を使う。回転 `r` を後から変えても注釈が正しい位置に残るのはこの持ち方だけ。

作業順:
- [ ] canvasの上にオーバーレイ（絶対配置の第2canvas or SVG）を重ね、`showPage()` のたびに注釈を再描画
- [ ] ツール: テキスト → 四角 → 線 の順（テキストが一番要求が強く、一番面倒）
- [ ] 選択・移動・削除（ヒットテスト）。まずは「直前に置いたものを消せる」だけでも実用になる
- [ ] プロパティ（色・サイズ）は最小限のパネル
- [ ] `buildPdf()` 内で焼き付け（`page.drawText()` / `drawRectangle()` / `drawLine()`）。**画面と保存結果の一致確認がこのフェーズの合否**
- [ ] テキスト入力中に Delete キーがページ削除に化けないよう、キーハンドラを入力中は無効化（現状 `index.html:513-523` は無条件）

### Phase 2 — 日本語フォントの結線
- [ ] `embedJpFont()` を `buildPdf()` から呼ぶ。**テキスト注釈が1つも無いときは呼ばない**（無駄なfetchと埋め込みを避ける）
- [ ] オフライン/CDN失敗時の扱いを決める（保存を中断するか、英数字だけ標準フォントで焼くか）→ 中断してメッセージが安全
- [ ] 画面プレビュー側もWebフォントで同じ Noto Sans JP を使い、**見た目と焼き付け結果をずらさない**
- [ ] README の「日本語フォント埋め込み [x]」は、Phase 2 完了までは「基盤のみ」と書く

### Phase 3 — ビューア強化（要判断・§6）
「プレビュー相当」を名乗るなら不足しているのは主にここ。
- [ ] ズーム（フィット / 100% / ±）、連続スクロール表示、ページ内スクロール
- [ ] テキスト選択・コピー（pdf.js の textLayer）
- [ ] 検索

### Phase 4 — オフライン / PWA
- [ ] pdf.js・pdf-lib・fontkit・Noto Sans JP を**リポジトリに同梱**（CDN依存のままでは Service Worker との相性もオフライン動作も詰む）
- [ ] `manifest.json` + `file_handlers` でPDFの既定アプリを狙う
- [ ] Service Worker でオフラインキャッシュ

### Phase 5 — Tauri化（デスクトップが欲しくなったら）
- [ ] 同じ `index.html` をWebViewに読ませ、ファイルダイアログだけネイティブに差し替え

## 5. テスト観点（`CLAUDE.md` の項目＋今回追加分）

手動チェックリスト（各フェーズ後に流す）:
1. 複数ファイルを開いて結合できる
2. 選択を保持したまま連続D&Dしても順序がズレない ※ロジックは検証済み、UI経路は要確認
3. 回転をUndoで正しく戻せる
4. 印刷プレビューの用紙サイズが元PDFと一致する
5. **「PDFを追加」の直後にUndo/Redoしても壊れない**（B-1の回帰テスト）
6. **壊れたPDF・パスワード付きPDFを掴んでもUIが固まらない**（B-2）
7. **注釈を付けたページを移動・回転・削除しても注釈が正しく追従する**（Phase 1）
8. **画面の注釈位置と保存後PDFの位置が一致する**（Phase 1）

自動化は Phase 1 完了後に Playwright のスモークテスト（開く→並び替え→保存→ページ数検証）を1本だけ入れるのが費用対効果が良い。単一HTML主義は崩さず、テストだけ別ディレクトリに置く。

## 6. 要判断（着手前に決めたい）

1. **ビューア強化（Phase 3）をやるか。** 「ページ編集ツール」に徹するなら不要。プレビューの置き換えを狙うならズームと検索は必須。→ Phase 1 完了後に判断で良い。
2. **依存のローカル同梱に踏み切るか。** 単一ファイルの手軽さは減るが、オフライン動作・起動速度・CDN消滅リスクが解決する。Phase 4 の前提でもある。
3. **書き込みツールの範囲。** テキスト／四角／線までか、ハイライト・フリーハンド・画像（署名）まで広げるか。まずは3種で止めるのを推奨。
